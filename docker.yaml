version: '3.8'

services:
  tiktok-app:
    image: sonabhandari20/tiktok
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8084:8084"
    volumes:
      - ./target:/app
    environment:
      - JAVA_OPTS=-Xmx512m
    restart: always
